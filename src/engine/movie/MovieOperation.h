#pragma once

/*
#define MACRO_OPERATION_IS_COMPLETE bool IsComplete() override { return mIsComplete; }

typedef struct SpriteBatch SpriteBatch;

typedef struct MovieOperation
{
	virtual bool IsComplete() = 0;
	virtual void Update();
	virtual void DrawHud(SpriteBatch* spriteBatch);
	virtual void SpeedUp();
	virtual bool ForcesWait();
	virtual bool DoesNotBlock();
	virtual void Broadcast(const char* key, const char* value);
};*/